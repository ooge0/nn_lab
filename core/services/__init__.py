"""
core.services
=============

Orchestration layer (``ExperimentRunner``, ``MetricsEngine``) built entirely on
``core.domain`` interfaces. Emits progress events; performs no I/O rendering
and no direct calls to concrete adapters.
"""
